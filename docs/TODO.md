# Openza Tasks Roadmap Notes

The active product is now the WinUI 3 app under `src/`.

## Deferred From WinUI V1

- standalone `openza-sync` service or CLI
- Obsidian adapter
- task tabs
- full-text search
- nested project tree
- reminders and notifications
- time tracking UI
- task enhancement UI
- active Linux/macOS maintenance

Legacy Flutter/Linux/Rust-FFI notes are preserved in git history and older releases.
