# Legacy Rust Sync Reference

This folder contains the previous Rust FFI sync engine from the Flutter implementation.

It is kept as migration reference only. The WinUI app does not build, package, or ship this Rust library. The active sync engine lives in C# under `src/Openza.Tasks.Core/Sync`.
